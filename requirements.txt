# Python dependencies for HDS_W5_cyy36 — peripheral blood cell classification.
#
#   python -m venv .venv && source .venv/bin/activate    # Python 3.12
#   pip install -r requirements.txt
#
# pip selects the correct platform wheel automatically, so this installs on
# Linux / macOS / Windows. (environment.yml is a macOS-arm64 conda lockfile and
# will NOT `conda env create` on other platforms — use this file for portability.)

# ── Core scientific stack ──────────────────────────────────────────────────
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1
matplotlib==3.10.9
seaborn==0.13.2

# ── Deep backbones (frozen feature extraction; optional fine-tuning) ────────
torch==2.5.1
torchvision==0.20.1
timm==1.0.27               # ViT-S/16 (generic-ViT control)
huggingface-hub==1.15.0   # DinoBloom-S weights

# ── Downstream classifiers + evaluation ─────────────────────────────────────
scikit-learn==1.8.0
xgboost==3.2.0
optuna==4.8.0             # hyperparameter tuning (TPE)
statsmodels==0.14.6      # McNemar's test

# ── Handcrafted features + segmentation ─────────────────────────────────────
opencv-python==4.13.0.92
scikit-image==0.26.0
pillow==12.2.0
cellpose==4.1.1          # whole-cell segmentation (CellPose-SAM)

# ── Explainability ──────────────────────────────────────────────────────────
shap==0.51.0
umap-learn==0.5.12

# ── I/O + tests ─────────────────────────────────────────────────────────────
openpyxl==3.1.5          # comparison.xlsx writer
pytest==7.4.4
